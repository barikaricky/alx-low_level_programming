#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int n;

    n = 98;

    printf("Correct output with n = %d\n", n);

    n = -98;

    printf("Correct output with n = %d\n", n);

    n = 980;

    printf("Correct output with n = %d\n", n);

    return (0);
}
